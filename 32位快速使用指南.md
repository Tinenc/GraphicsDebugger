# TinecmaTools 32位版本快速使用指南

## 🎯 快速开始（推荐）

### 步骤1：选择合适的版本

```
📁 H:\renderdoc-1.42\Win32\
├── 📂 Development\          ← 调试版本（54.7 MB）
└── 📂 Release\              ← 发布版本（21.8 MB）⭐ 推荐日常使用
```

**推荐使用 Release 版本**：
- ✅ 文件更小（21.8 MB vs 54.7 MB）
- ✅ 运行速度更快
- ✅ 经过优化，更稳定

---

## 🚀 使用场景

### 场景1：启动32位游戏并捕获帧

```powershell
# 进入 Release 目录
cd H:\renderdoc-1.42\Win32\Release

# 启动 UI 程序
.\qTinecmaTools.exe

# 在UI中：
# 1. 点击 "Launch Application"
# 2. 选择你的32位游戏exe
# 3. 点击 "Launch" 启动游戏
# 4. 在游戏中按 F12 或 Print Screen 捕获帧
```

### 场景2：注入到正在运行的32位进程

```powershell
# 方式A: 使用项目工具（推荐）
cd H:\renderdoc-1.42
.\tools\inject_dll.ps1 -ProcessName "your_game.exe" -DllPath "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"

# 方式B: 使用命令行工具
cd H:\renderdoc-1.42\Win32\Release
.\TinecmaToolscmd.exe --inject your_game.exe
```

### 场景3：全局钩子（同时支持32位和64位）

#### 配置全局钩子目录

```
部署目录\
├── x86\                     ← 32位文件
│   ├── TinecmaTools.dll
│   ├── TinecmaToolsshim32.dll  ⭐
│   ├── TinecmaToolscmd.exe
│   └── (所有依赖DLL)
│
└── x64\                     ← 64位文件
    ├── TinecmaTools.dll
    ├── TinecmaToolsshim64.dll
    ├── TinecmaToolscmd.exe
    └── (所有依赖DLL)
```

#### 复制文件命令

```powershell
# 创建全局钩子目录
$hookPath = "C:\TinecmaGlobalHook"
New-Item -ItemType Directory -Path "$hookPath\x86" -Force
New-Item -ItemType Directory -Path "$hookPath\x64" -Force

# 复制32位文件
Copy-Item "H:\renderdoc-1.42\Win32\Release\*" -Destination "$hookPath\x86" -Recurse -Force

# 复制64位文件
Copy-Item "H:\renderdoc-1.42\x64\Development\*" -Destination "$hookPath\x64" -Recurse -Force

Write-Host "全局钩子目录已配置完成: $hookPath" -ForegroundColor Green
```

---

## 🎮 针对不同游戏的使用策略

### 32位游戏（必须使用32位版本）

**示例游戏**: 老游戏、部分独立游戏

```powershell
# 使用32位版本
cd H:\renderdoc-1.42\Win32\Release
.\qTinecmaTools.exe

# 或使用命令行
.\TinecmaToolscmd.exe --capture "game32.exe"
```

### 64位游戏（使用64位版本）

```powershell
# 使用64位版本
cd H:\renderdoc-1.42\x64\Development
.\qTinecmaTools.exe
```

### 不确定游戏架构？

```powershell
# 检查进程架构
Get-Process | Where-Object {$_.ProcessName -like "*game*"} | Select-Object Name, @{Name="架构";Expression={if($_.Path -and [System.IO.File]::Exists($_.Path)){[System.Reflection.AssemblyName]::GetAssemblyName($_.Path).ProcessorArchitecture} else {"未知"}}}

# 或简单方法：同时部署32位和64位
# 全局钩子会自动选择正确的版本
```

---

## 🛡️ 反作弊绕过最佳实践

### 1. 使用延迟注入

等待游戏启动后再注入，避开启动时的检测：

```powershell
# 启动游戏
Start-Process "game.exe"

# 等待10秒
Start-Sleep -Seconds 10

# 注入DLL
.\tools\inject_dll.ps1 -ProcessName "game.exe" -DllPath "...\TinecmaTools.dll" -Delay 0
```

### 2. 确认文件已重命名

检查是否所有文件都使用 TinecmaTools 命名：

```powershell
# 检查32位目录
Get-ChildItem "H:\renderdoc-1.42\Win32\Release" -Filter "*renderdoc*"

# 应该返回空，如果有结果说明有文件未重命名
```

### 3. 字符串特征清理

```powershell
# 对主DLL运行字符串替换工具
python H:\renderdoc-1.42\tools\string_replacer.py "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"

# 检查是否有"RenderDoc"字符串残留
findstr /C:"RenderDoc" "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"
# 如果没有输出，说明清理成功
```

### 4. 进程隐藏（高级）

如果游戏有进程扫描，使用进程隐藏工具：

```powershell
# 编译并使用进程隐藏DLL
cl /LD H:\renderdoc-1.42\tools\hook_process_list.cpp

# 注入隐藏DLL到游戏进程
.\tools\inject_dll.ps1 -ProcessName "game.exe" -DllPath ".\hook_process_list.dll"
```

---

## 📊 性能对比

### Development vs Release

| 特性 | Development | Release |
|------|-------------|---------|
| 文件大小 | 54.7 MB | 21.8 MB ⭐ |
| 启动速度 | 较慢 | 更快 ⭐ |
| 运行性能 | 较慢 | 优化 ⭐ |
| 调试信息 | ✅ 完整 | ❌ 无 |
| 适用场景 | 调试问题 | 日常使用 ⭐ |

**建议**: 
- 💡 日常使用 → Release
- 🐛 遇到崩溃或问题 → Development（包含调试符号）

---

## 🔍 常见问题排查

### Q1: 注入失败，提示"Access Denied"

```powershell
# 解决方案：以管理员身份运行
# 右键点击 PowerShell → "以管理员身份运行"

# 然后重新执行注入命令
.\tools\inject_dll.ps1 -ProcessName "game.exe" -DllPath "...\TinecmaTools.dll"
```

### Q2: 游戏启动后立即崩溃

```powershell
# 可能原因：游戏检测到注入
# 解决方案：使用延迟注入

# 1. 先正常启动游戏
Start-Process "game.exe"

# 2. 等待游戏完全加载（根据游戏调整时间）
Start-Sleep -Seconds 15

# 3. 再注入
.\tools\inject_dll.ps1 -ProcessName "game.exe" -DllPath "...\TinecmaTools.dll"
```

### Q3: 找不到Python36.dll

```powershell
# 解决方案：确保所有依赖在同一目录
# 检查是否有 python36.dll
Test-Path "H:\renderdoc-1.42\Win32\Release\python36.dll"

# 如果返回 False，从 Development 复制
Copy-Item "H:\renderdoc-1.42\Win32\Development\python36.dll" `
          -Destination "H:\renderdoc-1.42\Win32\Release\python36.dll"
```

### Q4: 全局钩子不工作

```powershell
# 检查是否有 TinecmaToolsshim32.dll
Test-Path "H:\renderdoc-1.42\Win32\Release\TinecmaToolsshim32.dll"

# 应该返回 True
# 如果是 False，说明编译不完整，需要重新编译
```

### Q5: 游戏仍然检测到调试工具

```bash
# 进一步隐藏措施：

# 1. 再次确认文件重命名
dir H:\renderdoc-1.42\Win32\Release | findstr /i "renderdoc"

# 2. 字符串替换
python tools\string_replacer.py Win32\Release\TinecmaTools.dll

# 3. 使用虚拟机（终极方案）
# 在虚拟机中运行游戏和 TinecmaTools
```

---

## 📂 目录结构参考

### 最小部署包（仅32位）

如果只需要32位版本，最小需要这些文件：

```
部署目录\
├── TinecmaTools.dll                 ⭐ 主DLL
├── TinecmaToolsshim32.dll           ⭐ 全局钩子
├── qTinecmaTools.exe                ⭐ UI程序
├── TinecmaToolscmd.exe              ⭐ 命令行工具
├── Qt5Core.dll                      必需
├── Qt5Gui.dll                       必需
├── Qt5Widgets.dll                   必需
├── Qt5Network.dll                   必需
├── Qt5Svg.dll                       必需
├── python36.dll                     必需
├── python36.zip                     必需
├── d3dcompiler_47.dll               必需
├── dbghelp.dll                      必需
├── qtplugins\                       必需（Qt插件目录）
├── pymodules\                       可选（Python模块）
└── renderdoc_app.h                  可选（开发头文件）
```

### 复制最小部署包

```powershell
# 创建最小部署目录
$deployPath = "C:\TinecmaTools_x86_Minimal"
New-Item -ItemType Directory -Path $deployPath -Force

# 复制核心文件
$coreFiles = @(
    "TinecmaTools.dll",
    "TinecmaToolsshim32.dll",
    "qTinecmaTools.exe",
    "TinecmaToolscmd.exe",
    "TinecmaToolsui.exe",
    "Qt5Core.dll",
    "Qt5Gui.dll",
    "Qt5Widgets.dll",
    "Qt5Network.dll",
    "Qt5Svg.dll",
    "python36.dll",
    "python36.zip",
    "d3dcompiler_47.dll",
    "dbghelp.dll",
    "symsrv.dll",
    "renderdoc_app.h",
    "TinecmaTools.json"
)

foreach ($file in $coreFiles) {
    Copy-Item "H:\renderdoc-1.42\Win32\Release\$file" -Destination $deployPath -ErrorAction SilentlyContinue
}

# 复制目录
Copy-Item "H:\renderdoc-1.42\Win32\Release\qtplugins" -Destination "$deployPath\qtplugins" -Recurse
Copy-Item "H:\renderdoc-1.42\Win32\Release\pymodules" -Destination "$deployPath\pymodules" -Recurse

Write-Host "最小部署包已创建: $deployPath" -ForegroundColor Green
```

---

## 🎯 实战示例

### 示例1：捕获32位DX11游戏

```powershell
# 1. 进入Release目录
cd H:\renderdoc-1.42\Win32\Release

# 2. 启动UI
.\qTinecmaTools.exe

# 在UI中操作：
# - File → Launch Application
# - Executable Path: C:\Games\YourGame32bit\game.exe
# - Working Directory: C:\Games\YourGame32bit\
# - Capture Options: 勾选 "Allow Fullscreen"
# - Launch
# 
# 游戏中按 F12 捕获帧
```

### 示例2：命令行批量捕获

```powershell
cd H:\renderdoc-1.42\Win32\Release

# 启动游戏并自动捕获第一帧
.\TinecmaToolscmd.exe --capture-frames 1 --exe "game.exe"

# 捕获前100帧
.\TinecmaToolscmd.exe --capture-frames 1-100 --exe "game.exe"

# 捕获并保存到指定位置
.\TinecmaToolscmd.exe --capture-file "C:\Captures\game_frame.rdc" --exe "game.exe"
```

### 示例3：延迟注入避开反作弊

```powershell
# 创建延迟注入脚本 (inject_delayed.ps1)
@"
# 启动游戏
Write-Host "正在启动游戏..." -ForegroundColor Yellow
Start-Process "game.exe" -WorkingDirectory "C:\Games\YourGame"

# 等待游戏启动
Write-Host "等待游戏初始化（15秒）..." -ForegroundColor Yellow
Start-Sleep -Seconds 15

# 查找游戏进程
`$proc = Get-Process | Where-Object {`$_.ProcessName -like "*game*"}
if (`$proc) {
    Write-Host "找到游戏进程: `$(`$proc.ProcessName)" -ForegroundColor Green
    
    # 注入DLL
    Write-Host "正在注入 TinecmaTools..." -ForegroundColor Yellow
    & "H:\renderdoc-1.42\tools\inject_dll.ps1" \`
        -ProcessName "`$(`$proc.ProcessName).exe" \`
        -DllPath "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"
    
    Write-Host "注入完成！可以开始捕获了。" -ForegroundColor Green
} else {
    Write-Host "错误: 未找到游戏进程" -ForegroundColor Red
}
"@ | Out-File -FilePath "inject_delayed.ps1" -Encoding UTF8

# 执行延迟注入
.\inject_delayed.ps1
```

---

## 🔗 相关文档

- 📘 **完整编译说明**: `32位编译完成说明.md`
- 📕 **反作弊绕过技术**: `bypass_anticheat_guide.md`
- 📗 **项目结构说明**: `PROJECT_STRUCTURE.md`
- 📙 **快速开始指南**: `QUICK_START.md`
- 📓 **完整README**: `README.md`

---

## ✅ 使用前检查清单

- [ ] 确认使用正确的架构（32位游戏用x86，64位游戏用x64）
- [ ] 确认所有 TinecmaTools 文件在同一目录
- [ ] 确认没有 "RenderDoc" 残留文件名
- [ ] 以管理员身份运行（如果需要注入）
- [ ] 关闭或禁用杀毒软件（可能误报）
- [ ] 备份游戏存档（以防万一）

---

## 💡 提示

1. **选择版本**: Release 版本更快更小，日常使用推荐
2. **架构匹配**: 32位游戏必须用32位DLL，64位游戏必须用64位DLL
3. **延迟注入**: 对于有反作弊的游戏，等10-15秒后再注入
4. **文件完整**: 确保所有Qt和Python依赖都在同一目录
5. **管理员权限**: 注入需要管理员权限

---

**创建日期**: 2026-02-09  
**状态**: ✅ 可用  
**支持架构**: x86 (32位)
