# 解决全局钩子"短路径"问题

## ❌ 问题原因

错误信息：
```
Aborting. Couldn't start global hook.
File I/O failed: RenderDoc is installed on a volume or system that has short paths disabled.
For the global hook, short paths must be enabled where RenderDoc is installed.
```

**原因**: H盘禁用了 8.3 短文件名支持，而 RenderDoc 的全局钩子功能需要短路径。

---

## ✅ 解决方案

### 方案1：启用H盘的短路径（已执行）

我已经为你启用了H盘的短路径支持。现在需要：

#### 步骤1：验证短路径已启用

```powershell
# 以管理员身份运行 PowerShell
fsutil 8dot3name query H:
```

应该显示：`卷的状态为: 0 (8dot3 名称创建已启用)`

#### 步骤2：重新打开 qTinecmaTools.exe

```powershell
cd H:\renderdoc-1.42\Win32\Release
.\qTinecmaTools.exe
```

然后尝试启用全局钩子。

---

### 方案2：复制到C盘（备用方案，推荐）

如果方案1不行，将TinecmaTools复制到C盘（C盘通常默认启用短路径）：

```powershell
# 创建C盘目录
$targetPath = "C:\TinecmaTools"
New-Item -ItemType Directory -Path $targetPath -Force

# 复制32位文件
New-Item -ItemType Directory -Path "$targetPath\x86" -Force
Copy-Item "H:\renderdoc-1.42\Win32\Release\*" -Destination "$targetPath\x86" -Recurse -Force

# 复制64位文件
New-Item -ItemType Directory -Path "$targetPath\x64" -Force
Copy-Item "H:\renderdoc-1.42\x64\Development\*" -Destination "$targetPath\x64" -Recurse -Force

Write-Host "已复制到 C:\TinecmaTools" -ForegroundColor Green
```

然后从C盘运行：

```powershell
cd C:\TinecmaTools\x86
.\qTinecmaTools.exe
```

---

### 方案3：使用直接注入（不需要全局钩子）

如果不想用全局钩子，可以使用直接注入方式：

```powershell
# 方式1：启动时注入
cd H:\renderdoc-1.42\Win32\Release
.\qTinecmaTools.exe
# 在UI中选择 "Launch Application"，直接启动游戏

# 方式2：手动注入
cd H:\renderdoc-1.42
.\tools\inject_dll.ps1 -ProcessName "game.exe" -DllPath "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"
```

**优点**: 
- ✅ 不需要短路径支持
- ✅ 更隐蔽（反作弊检测更难）
- ✅ 可以延迟注入

**缺点**:
- ❌ 需要手动选择每个游戏
- ❌ 无法自动捕获所有3D应用

---

## 🔍 验证短路径是否工作

### 测试短路径

```powershell
# 查看目录的短路径名
cmd /c "dir /x H:\renderdoc-1.42"

# 应该看到类似这样的输出：
# 2024-02-09  RENDER~1.42  renderdoc-1.42
```

如果看到 `RENDER~1.42` 这样的短文件名，说明短路径已启用。

### 测试全局钩子

```powershell
# 1. 启动UI
cd H:\renderdoc-1.42\Win32\Release
.\qTinecmaTools.exe

# 2. 在UI中：
#    Tools → Settings → General
#    勾选 "Enable Global Process Hook"
#    点击 "Enable"

# 3. 如果没有报错，说明成功了！
```

---

## 📊 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **启用短路径** | 原地修复，不需要移动文件 | 需要管理员权限，可能影响性能 | ⭐⭐⭐ |
| **复制到C盘** | C盘通常默认支持短路径，更可靠 | 占用C盘空间（约200MB） | ⭐⭐⭐⭐⭐ |
| **直接注入** | 不需要全局钩子，更隐蔽 | 需要手动操作每个游戏 | ⭐⭐⭐⭐ |

---

## 🚀 快速命令（推荐流程）

### 最佳实践：复制到C盘

```powershell
# 一键复制并启动（以管理员身份运行PowerShell）

# 1. 复制文件
$target = "C:\TinecmaTools"
New-Item -ItemType Directory -Path "$target\x86" -Force
New-Item -ItemType Directory -Path "$target\x64" -Force
Copy-Item "H:\renderdoc-1.42\Win32\Release\*" -Destination "$target\x86" -Recurse -Force
Copy-Item "H:\renderdoc-1.42\x64\Development\*" -Destination "$target\x64" -Recurse -Force

# 2. 启动UI
cd C:\TinecmaTools\x86
Start-Process .\qTinecmaTools.exe

Write-Host "✅ 已从C盘启动 TinecmaTools" -ForegroundColor Green
Write-Host "现在可以尝试启用全局钩子了！" -ForegroundColor Yellow
```

---

## 💡 为什么全局钩子需要短路径？

**技术原因**:
1. 全局钩子需要在**系统级别**注册DLL路径
2. Windows系统API对长路径支持有限（MAX_PATH = 260字符）
3. 短路径（8.3格式）保证兼容性：
   - `H:\renderdoc-1.42\Win32\Release\` → `H:\RENDER~1.42\Win32\Release\`
   - 更短，更稳定，跨进程传递更可靠

---

## ⚠️ 注意事项

### 如果启用短路径后仍然失败

1. **重启计算机** - 让系统重新读取配置
2. **检查权限** - 确保以管理员身份运行
3. **使用方案2** - 直接复制到C盘
4. **使用方案3** - 放弃全局钩子，改用直接注入

### 短路径的副作用

启用短路径可能会：
- 轻微影响文件创建速度（需要生成额外的8.3文件名）
- 增加磁盘元数据（每个文件多存储一个短文件名）

**影响通常可忽略不计**。

---

## 📞 下一步

根据你的选择：

### 如果方案1成功
```powershell
# 从H盘启动
cd H:\renderdoc-1.42\Win32\Release
.\qTinecmaTools.exe
# 尝试启用全局钩子
```

### 如果选择方案2（推荐）
```powershell
# 执行上面的"一键复制并启动"脚本
# 然后从C盘启动
```

### 如果使用方案3
```powershell
# 直接注入方式
cd H:\renderdoc-1.42
.\tools\inject_dll.ps1 -ProcessName "your_game.exe" -DllPath "H:\renderdoc-1.42\Win32\Release\TinecmaTools.dll"
```

---

**创建时间**: 2026-02-09  
**状态**: 待测试  
**推荐方案**: 方案2（复制到C盘）
